# TODO

- [x] Fix bottom (“down”) sections styling issues
  - [x] Align footer styling by adding `.site-footer` rules to `public/css/style.css` (HTML uses `class="site-footer"`).
  - [ ] Make footer responsive consistent with existing media queries.
  - [ ] Remove/avoid conflicting `.footer` rules if they cause mismatch (keep for backward compatibility).
