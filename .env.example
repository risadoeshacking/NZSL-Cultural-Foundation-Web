# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/nz_cultural_events
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nz_cultural_events
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=development

# Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Image storage (Cloudinary) — required in production (Render's filesystem
# is wiped on every restart/redeploy). Leave unset for local development;
# uploads will fall back to local disk under UPLOAD_DIR.
# Get this from your Cloudinary dashboard: Account Details > API Environment variable
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>

# Admin Configuration
ADMIN_EMAIL=admin@nzevents.co.nz
ADMIN_PASSWORD=ChangeThisPassword123!

# Facebook Page Scraper (scripts/fb_import.py)
# Set FB_PAGE to your page name or URL slug — no login needed for public pages
FB_PAGE=NZSLCulturalFoundation
# Optional: add your personal FB login if the page blocks public scraping
# FB_EMAIL=you@email.com
# FB_PASSWORD=yourpassword